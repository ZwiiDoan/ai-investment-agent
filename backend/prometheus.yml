global:
  scrape_interval: 10s
  evaluation_interval: 10s

scrape_configs:
  - job_name: "otel-collector"
    static_configs:
      - targets: ["otel-collector:8889"]

  # Optional: scrape your FastAPI app if it exposes /metrics.
  # Replace service name and port with your container's.
  # - job_name: "app"
  #   static_configs:
  #     - targets: ["app:8000"]
